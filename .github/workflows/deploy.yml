script: |
  systemctl stop pm2-deploy.service || true
  fuser -k 3000/tcp || true
  cd /var/www/ai-nastoika-site
  git pull origin main
  npm ci
  npm run build
  pm2 restart /var/www/ai-nastoika-site/ecosystem.config.cjs --update-env